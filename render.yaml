services:
  - type: web
    name: cat-smart-operator
    runtime: node
    buildCommand: bash build.sh
    startCommand: node server/src/index.js
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHON_PATH
        value: python3
      - key: NODE_ENV
        value: production
    plan: free
